apiVersion: v1
kind: Service
metadata:
  name: notifications-service
  labels: 
    app.kubernetes.io/name: notificatiosn-service
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: notifications-service
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
